package leads

import (
	"context"
	"log"
)

func (s *service) RunRoAutomation(ctx context.Context) error {
	// TODO: actual logic
	log.Println("[Scheduler] Running monthly customer segmentation")

	//pageSize := 100
	//pageIndex := 0
	//
	//customers, err := s.customerVehicleRepo.GetCustomerVehicles(ctx, customervehicle.GetCustomerVehicleRequest{
	//	Limit:  pageSize + 1,
	//	Offset: pageSize * pageIndex,
	//})
	//if err != nil {
	//	return err
	//}

	return nil
}
